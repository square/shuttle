The following errors occured while we were importing your commit <%= @commit.revision[0,6] %> on <%= @commit.project.name %> into Shuttle:

SHA: <%= @commit.revision %>
Link: <%= project_commit_url(@commit.project, @commit) %>

Errors:

<% @commit.import_errors.each do |path, err| %>
<%= err %> (in <%= path %>)
<% end %>


Sincerely,
The Shuttle Automated Mailer
