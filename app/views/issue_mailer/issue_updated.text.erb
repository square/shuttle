<%= @issue.updater.name %> updated an issue on <%= l @issue.updated_at, format: :full %>:

Changes:

<% @issue.changes.each do |field, values| %>
<% unless Issue::SKIPPED_FIELDS_FOR_EMAIL_ON_UPDATE.include?(field) %>
<%= field.humanize %>:
    Was: <%= @translatable_fields.include?(field) ? t("models.issue.#{field}")[values.first] : values.first %>

    Is: <%= @translatable_fields.include?(field) ? t("models.issue.#{field}")[values.last] : values.last %>


<% end %>
<% end %>

View the issue: <%= project_key_translation_url(@project, @key, @translation) + "#issue-wrapper-#{@issue.id}" %>

Sincerely,
The Shuttle Automated Mailer
